[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "biomero-workflow-skills"
version = "0.4.0"
description = "Validated measurement-analysis skills from BIOMERO workflow repositories"
readme = "README.md"
requires-python = ">=3.10,<3.13"
license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]
authors = [
  { name = "NL-BioImaging" }
]
dependencies = [
  "PyYAML>=6.0,<7",
  "jsonschema>=4.23,<5"
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Image Processing"
]

[project.urls]
Homepage = "https://github.com/NL-BioImaging/BIOMERO.WorkflowSkills"
Repository = "https://github.com/NL-BioImaging/BIOMERO.WorkflowSkills"
Issues = "https://github.com/NL-BioImaging/BIOMERO.WorkflowSkills/issues"

[project.optional-dependencies]
test = [
  "build>=1.2",
  "mypy>=1.10",
  "pytest>=8",
  "pytest-cov>=5",
  "ruff>=0.5",
  "types-PyYAML>=6.0",
  "types-jsonschema>=4.23"
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.package-data]
biomero_workflow_skills = ["schemas/*.json"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.10"
strict = true
packages = ["biomero_workflow_skills", "omero_workflow_skills"]
